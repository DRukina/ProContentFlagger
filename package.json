{
  "name": "procontent-flagger",
  "version": "0.1.0",
  "description": "Advanced content moderation system with explainable AI",
  "private": false,
  "type": "module",
  "main": "dist/engine.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cli": "npm run build && node dist/cli.js",
    "test": "vitest run",
    "dev:watch": "tsc -p tsconfig.json --watch"
  },
  "keywords": ["content-moderation", "ai", "typescript", "text-analysis", "explainable-ai"],
  "author": "Claude Code",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.5.4",
    "typescript": "^5.6.2",
    "vitest": "^2.0.5"
  }
}